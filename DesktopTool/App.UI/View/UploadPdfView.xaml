<UserControl x:Class="DesktopTool.App.UI.View.UploadPdfView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopTool.App.UI.View"
             mc:Ignorable="d" 
             
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <local:NullToBoolConverter x:Key="NullToBoolConverter" />
    </UserControl.Resources>
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Upload PDF File" FontSize="16" FontWeight="Bold" Margin="0 0 0 10" />

            <Button Content="Select PDF File"
            Command="{Binding SelectFileCommand}"
            Width="150" Margin="0 0 0 10"/>

            <TextBlock Text="{Binding FileName}" FontStyle="Italic" />

            <Button Content="Upload PDF"
        Command="{Binding UploadCommand}"
        Width="150" Margin="0 10 0 10"
        IsEnabled="{Binding SelectedFilePath, Converter={StaticResource NullToBoolConverter}}" />

            <TextBlock Text="{Binding UploadProgress, StringFormat=Progress: {0}%}" Margin="0 0 0 5"/>
            <ProgressBar Value="{Binding UploadProgress}" Height="20" Maximum="100"/>
        </StackPanel>
    </Grid>
</UserControl>
